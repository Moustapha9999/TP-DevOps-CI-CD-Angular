import { FiltreRolePipe } from './filter-role.pipe';

describe('FiltreRolePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreRolePipe();
    expect(pipe).toBeTruthy();
  });
});
